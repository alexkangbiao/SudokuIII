#ifndef _SudokuBoardSector_H_#define _SudokuBoardSector_H_#include "cocos2d.h"USING_NS_CC;class SudokuBoardSector : public Ref{public:    SudokuBoardSector();    ~SudokuBoardSector();    static SudokuBoardSector* getInstance();        void setNumber(int number, int x, int y);    int  getNumber(int x, int y);        int * descriptionOfLineNo( int lineNo);    int * numberOfLineNo(int lineNo);    private:    int _sectorId;        //存放数字的指针，之所以不使用数字，应该是性能上考虑。    int *_numbers;        void init();};#endif