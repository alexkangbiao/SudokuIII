#ifndef _LOADING_SCENE_H_#define _LOADING_SCENE_H_#include "cocos2d.h"USING_NS_CC;enum LoadingSceneDrawingOrder : unsigned int {    LoadingSceneDrawingOrderBackground = 0,    LoadingSceneDrawingOrderLogo,    LoadingSceneDrawingOrderLoading};class LoadingScene : public Layer{public:    LoadingScene();    ~LoadingScene();        // there's no 'id' in cpp, so we recommend returning the class instance pointer    static Scene* createScene();    // Here's a difference. Method 'init' in cocos2d-x returns bool, instead of returning 'id' in cocos2d-iphone    virtual bool init() override;        void update(float dt) override;        // implement the "static create()" method manually    CREATE_FUNC(LoadingScene);    private:    // for init    void initBackgroundImage();    void initLoadingProgress();    //for user    void resetInitParameter();        void loadingImageSource(float dt);    void loadingMusicSource(float dt);    void loadingAnimation(float dt);    void loadingOthers(float dt);        void switchScene(float dt);            //记录loading内容的数量，来判断是否加载完成	float _loadedCount;    const float LOADED_COUNT = 4.0f;        ProgressTimer* _loadingProgress;    Label* _loadingLabel;    void updateLoading(float loadedCount);    };#endif